# Security

## Custom production rule

Production key rotation requires two maintainers and change ticket evidence.

## Session cookie contract

The authentication gateway owns `HttpOnly` and `SameSite=Strict` cookie policy.
